# make a program that ask the user two real numbers, than calculates the arithmetic mean

num1 = float(input('enter a number: '))
num2 = float(input('enter another number: '))

arith_mean = (num1+num2)/2
print(f'heres the mean {arith_mean}')
